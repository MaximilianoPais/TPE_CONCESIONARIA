<?php require './templates/layout/header.phtml' ?>

<div class="container mt-5">
    <h2>Editar moto</h2>
    <form action="router.php?action=editarMoto/<?= $moto->id_moto ?>" method="POST">
        <div class="mb-3">
            <label>Modelo</label>
            <input type="text" name="modelo" value="<?= $moto->modelo ?>" class="form-control" required>
        </div>
        <div class="mb-3">
            <label>Precio</label>
            <input type="number" name="precio" value="<?= $moto->precio ?>" class="form-control" step="0.01" required>
        </div>
        <div class="mb-3">
            <label>Categoría</label>
            <select name="id_tipo" class="form-select" required>
                <?php foreach ($categorias as $c): ?>
                    <option value="<?= $c->id_tipo ?>" <?= $c->id_tipo == $moto->id_tipo ? 'selected' : '' ?>>
                        <?= $c->nombre ?>
                    </option>
                <?php endforeach; ?>
            </select>
        </div>
        <div class="mb-3">
            <label>Imagen (URL opcional)</label>
            <input type="text" name="imagen" value="<?= $moto->imagen ?>" class="form-control">
        </div>
        <button type="submit" class="btn btn-success">Actualizar</button>
    </form>
</div>

<?php require './templates/layout/footer.phtml' ?>
